This is an automatic email sent by the {{ config('const.site_title') }} SYSTEM.

=================================================

@yield('body')

=================================================

{{ config('const.site_title') }}
{{ config('const.site_url') }}
